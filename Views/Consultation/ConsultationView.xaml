<UserControl x:Class="Patients.Views.Consultation.ConsultationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Margin="20">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>

            <TextBlock Text="Gestion des Consultations &amp; Ordonnances" 
                       FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

            <!-- RENDEZ-VOUS CONCERNE (obligatoire) -->
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Background="#FAFAFA" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="0. Rendez-vous concerné" FontSize="16" FontWeight="SemiBold" Foreground="#8E44AD" Margin="0,0,0,10"/>
                    <ComboBox x:Name="CbRendezVous" DisplayMemberPath="PatientNom" SelectionChanged="CbRendezVous_SelectionChanged"/>
                    <TextBlock x:Name="LblDetailRdv" FontSize="11" Foreground="#7F8C8D" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- INFORMATIONS DE CONSULTATION -->
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Background="#FAFAFA" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="1. Consultation Médicale" FontSize="16" FontWeight="SemiBold" Foreground="#2980B9" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Numéro Consultation -->
                        <TextBlock Text="N° Consultation :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtNumeroConsultation" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <!-- Diagnostic -->
                        <TextBlock Text="Diagnostic :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtDiagnostique" Grid.Row="1" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>

                        <!-- Notes Médicales -->
                        <TextBlock Text="Notes Médicales :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtNotesMedicales" Grid.Row="2" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- DOSSIER MÉDICAL -->
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Background="#FAFAFA" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="2. Informations du dossier médical" FontSize="16" FontWeight="SemiBold" Foreground="#27AE60" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="N° Dossier patient :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtNumeroDossier" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Poids (kg) :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtPoids" Grid.Row="1" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Taille (cm) :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtTaille" Grid.Row="2" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Groupe sanguin :" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtGroupeSanguin" Grid.Row="3" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Allergies :" Grid.Row="4" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtAllergies" Grid.Row="4" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Traitement en cours :" Grid.Row="5" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtTraitementDossier" Grid.Row="5" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>

                        <TextBlock Text="Antécédents :" Grid.Row="6" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtAntecedents" Grid.Row="6" Grid.Column="1" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- PRESCRIPTION (ORDONNANCE) -->
            <Border BorderBrush="#BDC3C7" BorderThickness="1" CornerRadius="5" Padding="15" Background="#FAFAFA">
                <StackPanel>
                    <TextBlock Text="3. Ordonnance / Prescription (Optionnel)" FontSize="16" FontWeight="SemiBold" Foreground="#27AE60" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Numéro Prescription -->
                        <TextBlock Text="N° Prescription :" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtNumeroPrescription" Grid.Row="0" Grid.Column="1" Margin="0,5" Padding="5"/>

                        <!-- Traitement -->
                        <TextBlock Text="Traitement prescrit :" Grid.Row="1" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
                        <TextBox x:Name="TxtTraitement" Grid.Row="1" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5" Padding="5"/>

                        <!-- Durée -->
                        <TextBlock Text="Durée du traitement :" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                        <TextBox x:Name="TxtDuree" Grid.Row="2" Grid.Column="1" Margin="0,5" Padding="5"/>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock x:Name="LblMessage" TextWrapping="Wrap" Margin="0,12,0,0" FontSize="12"/>

            <!-- BOUTONS D'ACTION -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button x:Name="BtnRappels" Content="🔔 Générer Rappels RDV (24h)" Background="#E67E22" Foreground="White" Padding="12,8" Margin="0,0,10,0" Click="BtnRappels_Click"/>
                <Button x:Name="BtnEnregistrer" Content="💾 Enregistrer la Consultation" Background="#2980B9" Foreground="White" FontWeight="Bold" Padding="15,8" Click="BtnEnregistrer_Click"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
