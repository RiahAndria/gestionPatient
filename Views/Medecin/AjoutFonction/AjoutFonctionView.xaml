<Window x:Class="Patients.Views.Medecin.AjoutFonction.AjoutFonctionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Width="300" SizeToContent="Height" Title="Ajout de fonction"> 
    <StackPanel Background="#F9FAFB" >
        <Border BorderBrush="#E5E7EB" BorderThickness="1" Background="White" CornerRadius="6" Padding="15">
            <StackPanel>
                <TextBlock Text="Ajouter une nouvelle fonction" FontSize="18" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,10"/>
            
                <TextBox x:Name="TXTFonctionAjouter" Height="32" Padding="6" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Click="fermer_ajoutFonction" FontSize="15" Height="32" Foreground="#FFF" IsCancel="True">Annuler</Button>
                    <Button Grid.Column="2" Click="ajout_nouvelle_fonction" FontSize="15" Background="blue" Height="32" Foreground="#FFF" IsDefault="True">Ajouter</Button>
                </Grid>
                
                <TextBlock x:Name="TXTMessageAjoutFonction" Text="" FontSize="12" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Window>