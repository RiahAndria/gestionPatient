<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <!-- EF Core (Microsoft.EntityFrameworkCore*, Npgsql.EntityFrameworkCore.PostgreSQL)
       retire : personne ne l'utilise (verifie sur tout le depot), equipe
       alignee sur ADO.NET + Dapper. Data/AppDbContext.cs doit aussi etre
       supprime  -->
  <ItemGroup>
    <PackageReference Include="Dapper" Version="2.1.79" />
    <PackageReference Include="MaterialDesignThemes" Version="5.3.2" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.10" />
    <PackageReference Include="Npgsql" Version="10.0.3" />
    <Resource Include="Assets\bg.jpg" />
  </ItemGroup>

</Project>
